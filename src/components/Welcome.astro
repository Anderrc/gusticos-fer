---

---

<div class='container'>
	<!-- Left section: Logo and text -->
	<div class='header'>
		<img src='/gflogo.png' alt='Gusticos Fer Logo' class='logo' />
		<h1>
			Descubre lo que te define. Moda, belleza y estilo en un solo lugar.
		</h1>
		<p>Todo lo que amas, en un solo lugar.</p>
	</div>

	<!-- Right section: Links -->
	<nav class='links'>
		<a href='#'>Ropa interior</a>
		<a href='#'>Pijamas</a>
		<a href='#'>Accesorios</a>
		<a href='#'>Cuidado capilar</a>
		<a href='#'>Ropa para ella</a>
		<a href='#'>Ropa para él</a>
	</nav>
</div>

<style>
	:root {
		--primary-color: #6a5acd; /* Tono lila */
		--secondary-color: #4a4a4a; /* Gris oscuro */
		--text-color: #1d1d1b; /* Texto principal */
		--bg-color: #ffffff; /* Blanco */
		--hover-color: #574ae2; /* Color hover */
	}

	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	body {
		font-family: 'Public Sans Variable', sans-serif;
		background-color: var(--bg-color);
		color: var(--text-color);
		display: flex;
		justify-content: center;
		align-items: center;
		overflow-x: hidden;
		min-height: 100vh !important; /* Asegura que siempre llene la pantalla */
	}

	.container {
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 20px;
		width: 100%;
		max-width: 1200px;
		padding: 20px;
		min-height: 100vh; /* Asegura que llene toda la pantalla */
		box-sizing: border-box; /* Asegura que el padding no afecte el tamaño */
		overflow: hidden; /* Evita desbordes del contenido */
		margin-top: 20px;
	}

	.logo {
		width: 100%; /* Ajusta el ancho del logo al contenedor */
		max-width: 300px; /* Define un tamaño máximo para pantallas grandes */
		height: auto; /* Mantiene la proporción original del logo */
		object-fit: contain; /* Asegura que el logo nunca se recorte */
	}

	.header {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 10px;
		width: 100%;
		max-width: 520px; /* Controla el ancho máximo del contenido */
	}

	.header h1 {
		font-family: 'Cormorant Garamond', serif;
		font-size: 38px;
		font-weight: 400;
		color: var(--text-color);
	}

	.header p {
		width: 100%;
		font-size: 1.2rem;
		color: var(--text-color);
		text-align: center;
	}

	.links {
		display: flex;
		flex-direction: column;
		gap: 15px;
		width: 100%;
		max-width: 520px;
		border: 6px solid #e7deff;
		border-radius: 7px;
		padding: 50px;
	}

	.links a {
		text-decoration: none;
		padding: 15px;
		font-size: 1rem;
		color: var(--text-color);
		background-color: var(--bg-color);
		border: 1px solid black;
		/* border-radius: 8px; */
		text-align: center;
		transition: all 0.3s ease;
	}

	.links a:hover {
		background-color: var(--hover-color);
		color: var(--bg-color);
	}

	@media (min-width: 900px) {
		.container {
			flex-direction: row;
			justify-content: space-between;
			align-items: flex-start;
			gap: 40px;
		}

		.header {
			align-items: center;
		}
	}

	@media (max-width: 768px) {
		.logo {
			max-width: 250px; /* Reduce ligeramente el tamaño máximo en móviles */
		}

		.container {
			padding: 15px; /* Asegura que haya espacio en los bordes */
		}
	}
</style>
